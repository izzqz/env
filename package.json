{
  "name": "@izzqz/env",
  "version": "0.1.0",
  "main": "dist/env.cjs",
  "module": "dist/env.mjs",
  "types": "dist/env.d.ts",
  "exports": {
    ".": {
      "import": "./dist/env.mjs",
      "require": "./dist/env.cjs",
      "types": "./dist/env.d.ts"
    }
  },
  "scripts": {
    "build:cjs": "tsc -p config/tsconfig.cjs.json && mv dist/cjs/env.js dist/index.cjs",
    "build:esm": "tsc -p config/tsconfig.esm.json && mv dist/esm/env.js dist/index.mjs && mv dist/esm/env.d.ts dist/env.d.ts",
    "build": "rm -rf dist && mkdir dist && npm run build:cjs && npm run build:esm && rm -rf dist/cjs dist/esm",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.0.0"
  }
}
